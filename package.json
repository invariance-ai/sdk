{
  "name": "invariance-sdk-repo",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @invariance/sdk build",
    "test": "pnpm --filter @invariance/sdk test",
    "typecheck": "pnpm --filter @invariance/sdk typecheck"
  }
}
